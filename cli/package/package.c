




void package_main(char ** argv, int argc) {
    matte_t * m = matte_create();
    matte_set_io(m, NULL, NULL, NULL); // standard IO is fine
    matte_set_importer(m, NULL, NULL); // standard file import is fine

    

    
        
}
