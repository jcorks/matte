// The special values "true" and "false" are 
// treated as always-accessible by the runtime.
@a = true;

// Comparisons with Numbers are 
// an example of am expression that 
// can compute to a Boolean. 
@b = 1 > 4;

// Will print false.
print(message:b);
